/**
 *
 * @param {string} answer
 * @param {string} tag
 * @param {number} length
 * @returns {Array<"empty" | "correct" | "wrongLoc" | >}
 */
export const validateTag = (answer, tag, length) => {
  if (!tag) return "empty";
};
